@inject IMediator Mediator;

@code {
    private async Task<string> Fetch()
    {
        return (await Mediator.Send(new CalculateFailureRate())).ToString("0.00");
    }
}

<StatCard Title="Failure Rate" Rate="%" Stat=@Fetch />