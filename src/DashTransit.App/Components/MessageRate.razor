@inject IMediator Mediator;

@code {
    private async Task<string> Fetch()
    {
        return (await Mediator.Send(new CalculateMessageRate())).ToString("0");
    }
}

<FluentCard>
    <StatCard Title="Avg Messages Sent" Rate="msg/sec" Stat=@Fetch />
</FluentCard>
