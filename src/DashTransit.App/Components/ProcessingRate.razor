@inject IMediator Mediator;

@code {
    private async Task<string> Fetch()
    {
        return (await Mediator.Send(new CalculateProcessingRate())).TotalMilliseconds.ToString();
    }
}

<StatCard Title="Avg Processing Time" Rate="ms" Stat=@Fetch />