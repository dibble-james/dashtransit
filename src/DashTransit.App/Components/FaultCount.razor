@inject IMediator Mediator;

@code {
    private async Task<string> Fetch()
    {
        return (await Mediator.Send(new CountFaults())).ToString("0.00");
    }
}

<FluentCard>
    <StatCard Title="Faults" Rate="last 1hr" Stat=@Fetch />
</FluentCard>
